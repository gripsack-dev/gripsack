"""Executable-docs checker package (plan/0042 §E), split by
responsibility: `manifest` (classification), `extract` (site-doc
extraction + pairing), `fixtures` (scaffold repos + the packaged SDK
install), `evaluate` (the two evaluation levels + assertions + the
negative selfchecks). One CLI: scripts/check_examples.py."""
