# Guarantee ledger

One row per guarantee: what is promised, where it is enforced, what is
trusted, and what evidence backs it. Statuses: `proposed`,
`implemented-unverified`, `checked` (machine proof), `blocked`,
`superseded`. Populated with evidence, not intentions (hardening
handoff §8). Proof work is on the roadmap; today's evidence is
regression tests, explorers, models and e2e.
| ID | Claim (with exclusions) | Enforcement | Admission boundary | Trusted components | Coverage | Bridges | Calibration | Status |
|---|---|---|---|---|---|---|---|---|
| CLASSIFY-001 | The commit classifier decides by exact transaction identity only: Committed ⟺ current == target; Uncommitted ⟺ fresh machine with no current, or current == previous ≠ target (target precedence explicit); Ambiguous otherwise. Numeric ordering and Apply/Rollback labels never decide. Exclusion: classification is only as good as the marker parse (MARKER-PARSE-001) and the current-pointer read. | Verus proof over `classify`, `gripsack-policy/src/lib.rs`; production reconcile, both explorers and the repeated-crash model call the same function | marker admission (F2) establishes the facts; no hidden preconditions — total function over all `RecoveryFacts` | Verus 0.2026.09.06.8dea4a2, Z3 4.16.0, Rust 1.98.0, vstd =0.0.0-2026-09-06-0133 | universal over the admitted input domain | `journal::tests`, both explorers, torn-marker e2e | seeded ambiguity→committed mutant fails its named postcondition in `scripts/check_verus.sh` | **checked** |
| OWNERSHIP-001 | Copy/link authority: Fresh ⟺ nothing live; TakeOver ⟺ consent && something live; Satisfied ⟺ live == desired without consent; Update ⟺ live == the last managed write (never preserved drift) and ≠ desired; Preserve otherwise. Links: Refuse ⟺ foreign, unrecorded, unconsented; TakeOver ⟺ consent over foreign+unrecorded; Link otherwise. Exclusion: decisions are over admitted identities; observation and effects live in exec. | Verus proofs over `plan_copy`/`plan_link`, `gripsack-policy/src/ownership.rs`; the lineage explorer and all planning call the same functions | identities arrive as typed values from exec observations; no hidden preconditions — total over all inputs | same pinned Verus/Z3/Rust toolchain as CLASSIFY-001 | universal over the input domain | lineage explorer (materialized filesystems), ops VM harness, ownership e2e | the authority table is the contract; a drift-promotion mutant would fail `Update`'s biconditional | **checked** |
| MARKER-PARSE-001 | A run marker missing `previous_generation` never reaches the classifier; explicit `null` remains the legitimate fresh-machine form. | manual `Deserialize` for `RunMarker`, `journal/marker.rs` | `run_marker()` reader; torn/corrupt markers fail closed with the journal retained | serde_json | all marker bytes | parser tests (missing/null/value/duplicate/overflow/wrong-type); torn-marker e2e asserts parser rejection | pre-fix: missing field parsed as `null` (the F2 defect) — parser test fails there | implemented-unverified |
| GC-RECOVERY-001 | `gc` never deletes objects an unfinished recovery needs: pending recovery state blocks collection (dry-run included, nothing deleted), the current generation is never pruned, and the deletion set is exactly candidates-minus-roots and monotone in the roots. Exclusions: root COMPLETENESS at production time (a manifest missing a dependency) is a separate obligation; a precise classify-aware admission (a committed run's entries need no priors) is deferred. | Verus proofs over `admit_gc`/`plan_prune`/`plan_delete` + `lemma_delete_monotone`, `gripsack-policy/src/retention.rs`; `journal::pending_recovery` + the session-bound collector in `gripsack-exec/src/gc.rs` | `LifecycleSession` held; unreadable journal/quarantine/manifests and non-UTF-8 inventory fail closed before any plan | cap-std Dir listings, UTF-8 path admission | universal over admitted inventories | `gc::tests` (crash-window refusal both modes, quarantine, post-reconcile collection), e2e crash → gc → apply → gc | pre-0.40: gc collected journal-only priors; a `delete`-includes-roots mutant fails `plan_delete`'s extensional contract | **checked** |
| MERGE-SPLICE-001 | The managed-block splice keeps every foreign byte verbatim and in order: output == the prefix-recursive spec mirror exactly; a single-span identity replacement is the identity function; the complement intervals appear verbatim and in order. Exclusions: span admission only — UTF-8 boundary alignment of scan output is the parser's obligation (Layer 2, DEFERRED: the marker-grammar port exceeded its time-box; bound recorded per 0047 §4). The exec wrapper converts through `String::from_utf8` and fails closed, never panics. | Verus proof over `splice_bytes` (+ identity/gaps lemmas), `gripsack-policy/src/merge.rs`; `ManagedBlockSet::remove`/`upsert` call the kernel | `valid_spans`: sorted, non-overlapping, in-bounds — no hidden preconditions beyond the admission predicate | same pinned Verus/Z3/Rust toolchain as CLASSIFY-001 | universal over admitted (text, spans, replacement) | `ManagedBlockSet` unit tests, the merge fuzz target, `test_merge_boundaries.py` — unchanged, exercising the kernel path | a final-tail-dropping mutant fails the spec-mirror postcondition in `scripts/check_verus.sh` | **checked** |
| GRAPH-CLOSURE-001 | `build_closure` is exactly the build-reachable set: soundness (every member reachable, ghost witness paths) and completeness (every reachable name a member, worklist edge-closure + path induction), with termination on cyclic input (processed-set measure) and a duplicate-free result. `build_only_members` is exactly build-minus-runtime. Ordering-only edges are unrepresentable in the kernel's input view. | Verus proofs over `build_closure`/`build_only_members`, `gripsack-policy/src/graph.rs`; the name-index adapters in `gripsack-ir/src/dependencies.rs` call the kernels | edges in range; unknown dep names are sink nodes (the pre-0047 semantics, preserved) | same pinned toolchain as CLASSIFY-001 | universal over admitted graphs, cycles included | schedule.rs closure composition unchanged; journeys and e2e drive the kernel-derived closures | a visited-but-unrecorded mutant fails the result-membership contract in `scripts/check_verus.sh` | **checked** |
| WORKSPACE-ROLES-001 | Every admitted graph edge role projects to exact build/required-validation decisions: production and build input are build edges; validation is a mandatory publication gate; runtime, local ordering, task prerequisites and retention never become build edges. Exclusion: this does NOT prove JSON/schema decoding, name→index adaptation, source reference completeness, kind/binding metadata or realization. | Verus proof over `project_graph_roles`, `gripsack-policy/src/graph/roles.rs`; `gripsack-ir/src/sema/workspace/policy.rs` invokes that kernel and `build_closure` during v5 workspace admission and v4 read-only validation | Typed catalog names and role edges after the strict versioned reader; incomplete name/index correspondence remains open | pinned Verus/Z3/Rust toolchain as CLASSIFY-001 | Exact role classification over all finite typed edge inputs; target compatibility is a distinct proof below | The `policy/coverage/{declaration,diagnostics}.rs` source walk checks projected role, target, artifact selector, exported command, admissible kinds and target binding; dropped/reclassified/substituted edges fail E131 in Rust regressions, not as a Verus schema bridge | The dropped-validation mutant fails the named `required_validation` assertion in `graph/roles.rs`; other historical mutants still need plan/0048 §6 attribution hardening | **checked** (kernel only; A1-02 delivery remains in_progress) |
| WORKSPACE-INDEX-001 | A candidate catalog index binds a declared output name iff it is in range and the catalog name at that position is exactly the declaration; missing, out-of-range and wrong-name candidates fail. Distinct names with valid bindings cannot alias one index. The bound index has a private constructor; every index this workspace adapter places into build closure or uses as its root/producer comes from a successful binding. Exclusion: not a proof that serde decoded all references, `names::check` built a complete catalog, `graph::collect` retained every source edge or BTreeMap returned a candidate. | Verus proofs over `bind_output_index`, `distinct_names_have_distinct_indices` and the private `BoundOutputIndex` in `gripsack-policy/src/graph/name_index.rs`; production `gripsack-ir/src/sema/workspace/policy.rs` binds both endpoints of each projected edge and package root/producer before closure | `check_catalog` and `coverage::check` fail E131 on omitted/substituted sources; the unverified BTreeMap supplies only a candidate, never a trusted index | Pinned Verus/Z3/Rust toolchain; upstream serde/catalog/reference extraction remains trusted | Universal over borrowed typed names and arbitrary optional index candidates; Verus observed 72 verified/0 errors (all policy families), not a full schema/adapter theorem | The direct name-index runtime regression rejects aliased/out-of-range/missing candidates; seven production adapter tests and real CLI target paths exercise the wired flow | Forcing all in-range candidates to match fails the exact same-name assertion in `name_index.rs` with the verifier present | **checked** (pointwise name binding only; A1-02 remains in_progress) |
| WORKSPACE-TARGET-001 | A provider target satisfies its consumer exactly when declared OS, arch and ABI match and its normalized minimum OS does not exceed the consumer's (an absent provider floor accepts any consumer floor; a consumer with no floor rejects a provider floor). Missing ABI is not a wildcard. Exclusions: no proof of serde/schema-to-domain conversion, `valid_abi` admission, name→index mapping, `TargetBinding` metadata, fixed-prefix layout or native materialization. | `gripsack-policy/src/target.rs` proves `release_at_most` and `supports_target` against their semantic spec; production `gripsack-ir/src/sema/workspace/refs/bindings.rs` calls the same kernel for recipe producers and environment/image package selections; no second comparator remains | Typed v5 `WorkspacePlatform::policy_requirement` normalizes omitted patch to zero and maps OS/arch/ABI enums before the kernel; strict reader and `valid_abi` guard that boundary | Pinned Verus/Z3/Rust toolchain as CLASSIFY-001; IR enum conversion and schema parsing remain trusted | Universal pure kernel over typed provider/consumer requirements; fresh Verus observed 68 verified/0 errors, including the target obligations, and a real CLI minimum-OS/ABI case admits or rejects with both spans | Existing Rust target-floor/ABI and real CLI producer/selection cases exercise the conversion; no name/index or full adapter refinement theorem is claimed | Replacing the matching GNU/GNU pattern with GNU/MUSL fails the exact `abi_matches == (provider.abi == consumer.abi)` assertion at `target.rs` with the verifier present | **checked** (kernel only; A1-02 delivery remains in_progress) |
| DIAGNOSTIC-ALLOC-001 | The v5 TypeScript frontend only emits a structured diagnostic when its code is one of the five frontend allocations from `schema/diagnostics.json`; an unallocated ID carried by a user-thrown `DiagnosticError` stays a traceback, never an invented stable core E-code in terminal or `check --json`. Exclusions: this does not authenticate the author of an allocated code, prove diagnostic *classification* or prevent user-authored bytes appearing inside raw traceback text. | `scripts/gen_diagnostic_registry.py` emits `diagnosticCodes` and a static `Record<string, true>` membership table; `typescript/src/diagnostic.ts::isFrontendCode` bounds `asDiagnostic` at the frontend envelope boundary; the embedded SDK carries identical generated bytes | Registry validator rejects malformed/duplicate allocations; the Deno driver still distinguishes user/engine exceptions from structured authoring diagnostics | Python code generator and Deno runtime; parser/rendering and source-span containment remain separate contracts | Strict TypeScript frontend gate passed 63/63; real CLI fixture that previously rendered `error[E999]` now returns an empty structured diagnostic list with a traceback; full e2e 290/290 in Linux container | Rust core diagnostics keep their one representation; no additional IR wire field or release grant was introduced | Boolean registry version, duplicate core ID and duplicate frontend name mutants reject their named property; the real E999 fixture is a failing-before/passing-after transport negative, not a Verus classification theorem | **implemented-unverified** (A1-06 CI/proof/Mac and classification remain open) |
| HOST-NAME-001 | An explicit `--host`, `[env] default_host`, or sanitized detected hostname selects one host-file and lockfile *name*, never a path. Invalid names (empty, absolute, separator, leading dot, `..`, non-ASCII/control) fail E132 before evaluator/tool provisioning or host-derived I/O; `adopt` checks before writing any generated file. Both the executor context and linter receive the admitted selected name, so a direct caller cannot construct a traversal host via their typed APIs. Exclusions: this lexical rule does not resolve symlinked `hosts/` or `locks/` ancestors, protect against arbitrary external writers, or prove filesystem containment; repo trust, capability and persistence guarantees remain separate. | Private `HostName` in `gripsack-ir/src/host_name.rs` reuses E116's single-segment predicate; `commands/eval.rs` admits it before provisioning, `adopt/mod.rs` before generation; `gripsack-exec::{ctx,lockfile,source::preflight}` and `gripsack-lint::versions` require that type | CLI flag > env.toml default > ASCII-sanitized detected hostname, with no unchecked lockfile writer or linter path | Rust name grammar, serde config parser, frontend inputs transport and OS filesystem semantics | Finite Rust negative/positive name fixtures and sandboxed real CLI parent/absolute/adopt witnesses, safe dotted role and default-host version-aware lint; full-suite/source-bound protected CI and native Mac remain open | Admitted name serialized as a string into the one frontend inputs envelope; lockfile/linter use that exact selected value, not the optional raw CLI flag | Before: `update` succeeded with `../modules/role`, an absolute `--host` reached a victim lockfile outside the repo, and `adopt` generated repo files before noticing `../modules/evil`; after: all three return E132 before those effects | implemented-unverified |
| FRONTEND-BOUNDS-001 | One frontend probe fixpoint shares a ten-minute process budget; each invocation stops rather than parsing partial output once stdout or stderr exceeds 16 MiB. JSON line framing permits the full stdout limit, and at most the last 64 KiB of stderr is retained. The supervisor kills and reaps the process group on limit/deadline under its documented OS scheduling assumptions. Exclusions: Deno/tool provisioning before the fixpoint, external descendants escaping the process group, synchronous callback preemption, R5 identity/FD/grant/trace coverage and platform fault qualification are not proved. | `commands/frontend.rs::run_bounded` calls `gripsack-process::run`; `commands/probe.rs::eval_to_fixpoint` gives every round the remaining one-operation deadline and checks StopReason before JSON parse | Run the admitted command only after repo trust and HostName admission; the process supervisor owns stdout/stderr, deadline and group cleanup | OS process groups, monotonic clock, JSON parser and the existing R5 supervisor assumptions | Isolated real-CLI e2e reproduces former unbounded stdout/stderr and exercises both limit failures; normal real-Deno two-round probe flow passes | Rust process-supervisor limit/deadline tests cover mechanics; no cross-platform theorem or exact-commit CI yet | Original `.output()` returned only after collecting 16 MiB+1 bytes and reported malformed JSON, not a limit; both adversarial cases failed-before | implemented-unverified |
| EVAL-READ-GRANTS-001 | No path serialized into Deno's comma-delimited `--allow-read` list contains an ASCII comma: repository, host-input directory, embedded frontend and optional canonical proven-name `@gripsack/core` pin are all checked before child spawn. An invalid path reports E133 with the path and cannot split into an unrequested outside grant. A legitimate canonical package pin outside the repo retains its explicitly scoped read permission. Exclusions: a named package can still execute author-controlled code after explicit repo trust, grants are not a proof of arbitrary symlink-race containment or filesystem isolation, and this lexical rule does not discharge R1 source approval, §1.2 credential routing or other evaluator permissions. | `commands/frontend.rs::admit_read_grant` is the one shared lexical gate for every grant path in `Frontend::command`; `FrontendRunError::Grant` reaches the CLI diagnostic sink rather than spawning; `schema/diagnostics.json` allocates core-only E133 | Canonical optional pin checked before its package.json name admission, and base paths checked before formatting the one Deno permission flag; no alternate command caller | Deno 2.9.6 comma-list behavior observed with a real canary, OS path encoding and race assumptions, `pin_is_gripsack_core` package-name check | Real CLI pin-canary escape and comma-repo refusal witnesses, comma Gripsack home, `check --json` E133 parity, valid external pin and nonpackage-symlink controls; direct Rust command-construction cases cover all three built-in grant positions | Source-bound runner/CI/native Mac still open; no proof of all possible filesystem grants or internal Deno parser correctness | Before the fix, a canonical comma pin with a valid embedded SDK made the real CLI read a canary outside its authorized repo and exit 0; a comma repo failed later inside Deno rather than at E133 admission | implemented-unverified |
| ADOPT-TRUST-001 | `grip adopt` on an untrusted repo cannot inspect its target or generate payload, module or host source, even under `--yes`; repo-shape metadata and typed host admission may run first. A trusted repo retains the reversible adoption flow. Exclusions: `GRIPSACK_TRUST_ALL=1` is an explicit CI bypass; the current path-keyed trust contract is not source-bound R1 approval or a guarantee against external writers. | `commands/adopt/mod.rs::adopt` calls the existing `commands/mod.rs::trust_gate` before target and managed-state inspection or generation | Path-keyed trust admission after env-repo shape and HostName grammar, before mutable repo/target effects | `gripsack-store::trust::ensure_trusted`, filesystem and explicit operator trust | Real-CLI non-interactive untrusted case asserts unchanged host, no generated module/config and intact target; trusted adopt→rollback end-to-end still passes | Retains plan/0013 D7 trust policy; R1 source-bound approval remains open | Original untrusted `adopt --yes` changed the host before returning the `grip trust add` hint | implemented-unverified |
| SCHEDULER-001 | The scheduler's decisions are the proved transition system: a module starts only after every dependency finished OK (`remaining` is the exact not-yet-finished count), each module starts at most once, after any failure nothing further starts, and while no failure is latched every unblocked module is queued. Exclusions: the mutex/condvar bridge, thread mechanics and resource locks are TESTED, not proved (journeys, e2e) — the kernel proves decisions, not the concurrency protocol. | Verus proof over `PureScheduler` (`inv` preserved by `new`/`next`/`finish_ok`/`finish_fail` plus the safety readings), `gripsack-policy/src/schedule.rs`; production `run_all` routes every readiness/release/latch decision through the kernel, names↔indices at the boundary | edges in range and duplicate-free (the adapter dedups); indices name-sorted so kernel FIFO order == the pre-kernel name-sorted readiness | same pinned toolchain as CLASSIFY-001 | universal over admitted graphs | policy runtime smoke tests (diamond completion, latch), journeys, scheduler e2e — all drive the kernel-driven `run_all` | a latch-dropping mutant fails the named no-starts-after-failure postcondition in `scripts/check_verus.sh` | **checked** |

## Limits that stay visible

- The lifecycle lock serializes cooperating gripsack processes for one
  home. It excludes neither editors nor arbitrary external writers.
- A pinned parent capability prevents pathname-redirection races; it is
  not an atomic compare-and-swap of bytes/mode against all writers. The
  observation-to-write window is documented on the safety page.
- Hash equality assumes SHA-256 collision resistance for content
  identity; encoding injectivity (RECOVERY-IDENTITY-001) is separate
  and does not depend on it.
- Process-crash tests and power-loss models are different evidence:
  kernel caches surviving `kill -9` say nothing about physical storage
  failure; the TLA+/explorer power-loss lanes cover the reordering
  envelope, not the hardware.
- File/directory sync and rename semantics are trusted OS/filesystem
  contracts, qualified to the platforms CI exercises.
- The current-pointer switch is a logical generation commit; outside
  readers do not receive an atomic multi-file snapshot.
- Activation hooks can be replayed after a crash; neither
  exactly-once effects nor eventual hook success are promised.
- Verified or not, policy functions do not prove Deno, TLS, the
  compiler, the solver or storage hardware.
