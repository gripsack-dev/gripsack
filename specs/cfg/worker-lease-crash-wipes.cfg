\* Calibrated mutant: a crash that silently erases leases must be
\* visible — recovery may proceed as if nobody held one.
CONSTANTS
 TwoClients = TRUE
 CrashEnabled = TRUE
 StopIgnoresLeases = FALSE
 CrashWipesLeases = TRUE
SPECIFICATION Spec
INVARIANTS TypeOK NoSilentLeaseVanish
CHECK_DEADLOCK FALSE
