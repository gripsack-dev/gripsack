\* One generation only: every run after the first targets the
\* generation that is already current, so resume's generation
\* comparison — not a ghost invocation identity — carries the load.
CONSTANTS
 GENS = {1}
 NONE = NONE
SPECIFICATION Spec
INVARIANTS TypeOK InFlightHasRecord RunIdentity NoSilentSkip
PROPERTY RecoveryCompletes
CHECK_DEADLOCK FALSE
