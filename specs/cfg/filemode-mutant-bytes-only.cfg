\* CALIBRATED NEGATIVE: the pre-0043 template/merge identity seam —
\* satisfaction compared bytes only, so a chmod was invisible to reconcile
\* and prune (a chmoded managed file read "satisfied" and pruned as
\* intact). ChmodIsDrift must be violated by name.
CONSTANTS
 SURFACE = "template"
 SRC = m0755
 M_0644 = m0644
 M_0755 = m0755
 M_0600 = m0600
 NOFILE = NOFILE
 FIXED_TEMPLATE = FALSE
 BYTES_ONLY = TRUE
SPECIFICATION Spec
INVARIANT ChmodIsDrift
CHECK_DEADLOCK FALSE
