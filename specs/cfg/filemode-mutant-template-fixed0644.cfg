\* CALIBRATED NEGATIVE: the pre-0043 template seam — fresh templates
\* always landed 0644 regardless of the payload's mode (migration report
\* finding 2: a wrapper script lost its exec bit). ExecSurvivesDeploy must
\* be violated by name.
CONSTANTS
 SURFACE = "template"
 SRC = m0755
 M_0644 = m0644
 M_0755 = m0755
 M_0600 = m0600
 NOFILE = NOFILE
 FIXED_TEMPLATE = TRUE
 BYTES_ONLY = FALSE
SPECIFICATION Spec
INVARIANT ExecSurvivesDeploy
CHECK_DEADLOCK FALSE
